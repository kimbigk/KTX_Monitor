# KTX 취소표 감시 Android 앱

## PROJECT_PLAN.md

> 문서 버전: v1.0
> 프로젝트 목적: Android 스마트폰에서 KTX 승차권 잔여석을 지속적으로 감시하고, 사용자 조건에 맞는 좌석이 발견되면 즉시 알림하여 사용자가 직접 예매할 수 있도록 지원한다.

---

# 0. 반드시 지켜야 하는 개발 운영 규칙

## 0.1 Phase 단위 개발

이 문서는 Phase 0~8 전체 개발 계획을 정의한다.

그러나 **한 번의 작업에서 전체 Phase를 구현해서는 안 된다.**

Antigravity는 사용자가 명시적으로 지시한 현재 Phase만 수행해야 한다.

예:

```text
사용자:
PROJECT_PLAN.md를 기준으로 Phase 1만 수행해.
```

이 경우 Phase 1의 작업만 한다.

Phase 1을 완료했다고 판단해도 Phase 2를 자동으로 구현하지 않는다.

다음 Phase는 사용자가 별도로 지시했을 때만 시작한다.

---

## 0.2 Phase 범위 준수

현재 Phase의 목표에 필요하지 않은 기능을 미리 구현하지 않는다.

특히 다음과 같은 "미리 만들어 놓으면 편할 것 같다"는 이유의 선행 구현을 금지한다.

* 실제 코레일 조회
* 자동 로그인
* 자동 예약
* 자동 결제
* CAPTCHA 우회
* 매크로 탐지 회피
* 서버 구축
* 불필요한 외부 API 연동

---

## 0.3 기존 코드 보존

기존에 정상적으로 동작하는 코드는 이유 없이 재작성하지 않는다.

수정이 필요한 경우:

1. 현재 구조 확인
2. 영향 범위 확인
3. 최소 변경
4. 빌드
5. 테스트

순서로 작업한다.

대규모 리팩터링은 반드시 현재 Phase의 완료에 필요할 때만 한다.

---

# 1. 프로젝트 목표

## 1.1 최종 사용 흐름

최종 앱은 다음 흐름을 목표로 한다.

```text
사용자
  ↓
감시 대상 등록
  ↓
감시 시작
  ↓
Android 백그라운드 감시
  ↓
KTX 좌석 상태 확인
  ↓
사용자가 지정한 조건과 비교
  ↓
조건에 맞는 좌석 발견
  ↓
즉시 알림
  ↓
사용자가 알림 터치
  ↓
코레일 예매 화면 이동
  ↓
사용자가 최종 예약
```

---

# 2. 프로젝트의 핵심 원칙

## 2.1 모바일 단독 동작

최종 목표는 집의 PC를 켜놓지 않아도 되는 것이다.

Android 스마트폰 자체에서 감시 서비스를 실행한다.

```text
Android Phone
    ↓
Monitoring Service
    ↓
Monitoring Engine
    ↓
좌석 상태 판단
    ↓
Notification
```

---

## 2.2 사용자 개입을 최종 단계에 유지

앱의 핵심 역할은:

**발견 + 알림 + 예매 화면 연결**

이다.

다음 기능은 프로젝트 범위에서 제외한다.

* 자동 예약 확정
* 자동 결제
* CAPTCHA 우회
* 계정 인증 우회
* 자동 매크로 탐지 회피

사용자가 최종 예약 행동을 직접 수행한다.

---

# 3. 개발 기술

## 3.1 기본 기술

* Android
* Kotlin
* Jetpack Compose
* Material 3
* Gradle
* AndroidX

가능한 한 공식 Android 구성요소를 우선 사용한다.

---

## 3.2 데이터 저장

감시 대상 및 설정값은 로컬 저장한다.

우선 고려:

* Room
* DataStore

각 데이터 특성에 따라 적절한 방식을 선택한다.

---

## 3.3 백그라운드 실행

장시간 감시를 위해 Android Foreground Service 기반 구조를 사용한다.

단, Android 최신 버전의 서비스 실행 제한 및 권한 정책을 확인하여 버전에 맞게 구현한다.

---

# 4. 권장 아키텍처

최종적으로 다음과 같은 구조를 목표로 한다.

```text
UI
│
├── MainScreen
├── WatchTargetScreen
└── SettingsScreen
       │
       ▼
ViewModel
       │
       ▼
UseCase / Domain
       │
       ├── WatchTargetManager
       ├── MonitoringController
       └── SeatMatchingEngine
       │
       ▼
Repository
       │
       ├── Local Repository
       └── Monitoring Repository
       │
       ▼
Monitoring Service
       │
       ▼
Monitoring Engine
       │
       ▼
Notification Manager
```

Phase가 진행되면서 필요한 범위에서만 이 구조를 확장한다.

---

# 5. 기본 데이터 모델

감시 대상은 최소한 다음 정보를 가져야 한다.

```text
WatchTarget
 ├── id
 ├── departureStation
 ├── arrivalStation
 ├── date
 ├── startTime
 ├── endTime
 ├── trainType
 ├── seatType
 ├── passengerCount
 ├── enabled
 ├── lastCheckedAt
 ├── lastStatus
 └── lastFoundTrain
```

추후 필요하면 다음 정보를 추가할 수 있다.

```text
priority
preferredTrainNumbers
excludedTrainNumbers
notificationEnabled
```

단, 현재 Phase에 필요하지 않은 필드는 미리 추가하지 않는다.

---

# 6. 앱 주요 화면

최종 앱은 최소 다음 화면을 갖는다.

## Main

```text
KTX 취소표 감시

🟢 감시 중

등록된 감시 대상 2개

부산 → 서울
9/27
12:00 ~ 18:00
KTX
일반실
1명

[수정] [중지]

----------------

서울 → 부산
9/28
17:00 ~ 21:00
전체
일반/특실
1명

[수정] [중지]

----------------

[ + 감시 대상 추가 ]

최근 이벤트
...
```

---

## 감시 대상 추가/수정

입력:

* 출발역
* 도착역
* 날짜
* 시작 시간
* 종료 시간
* 열차 종류
* 좌석 등급
* 승객 수

---

## 설정

최소:

* 감시 주기
* 알림 사용
* 소리
* 진동
* 백그라운드 감시
* 배터리 최적화 안내

---

# 7. Phase 0 — 프로젝트 초기화

## 목표

개발 환경과 프로젝트 구조를 확정한다.

## 작업

* Android 프로젝트 생성 또는 기존 프로젝트 분석
* Kotlin 확인
* Gradle 구성 확인
* Compose 구성 확인
* 최소 지원 Android 버전 결정
* 패키지명 결정
* Git 저장소 확인
* 기본 빌드 확인

## 구현

기본 앱이 정상적으로:

```text
Build
↓
Install
↓
Launch
```

되는 것까지 확인한다.

## 구현하지 않을 것

* 실제 코레일 조회
* 데이터 저장
* 백그라운드 감시
* 알림
* 로그인
* 서버

## 완료 조건

* 프로젝트 빌드 성공
* 실제 Android 기기에서 실행 성공
* 기본 화면 표시
* 오류 없는 Gradle 빌드

## 다음 Phase 진입 조건

사용자가 명시적으로 Phase 1을 지시할 것.

---

# 8. Phase 1 — 앱 기본 구조 + 감시 대상 관리

## 목표

실제 코레일 기능을 제외한 앱의 기본 틀을 완성한다.

## 작업

### UI

* MainScreen
* WatchTargetAdd/EditScreen
* SettingsScreen

### 감시 대상

* 추가
* 수정
* 삭제
* 활성화/비활성화

### 저장

앱 종료 후에도 데이터 유지.

### 기본 상태

* 감시 중
* 감시 중지
* 감시 대상 개수
* 마지막 상태

---

## Mock 감시

실제 코레일에 연결하지 않는다.

MockMonitoringEngine을 구현한다.

예:

```text
check()
→ 검사 성공
```

또는 개발용 버튼:

```text
테스트 좌석 발견
```

을 통해 가상 좌석 발견 이벤트를 발생시킨다.

---

## 알림

테스트 좌석 발견 시 Android Notification을 발송한다.

예:

```text
KTX 좌석 발견

9/27 부산 → 서울
14:20 KTX
일반실
```

---

## 백그라운드 구조

Foreground Service의 기본 구조만 만든다.

실제 조회는 하지 않는다.

---

## 재부팅 구조

BOOT_COMPLETED 처리 구조를 준비한다.

활성 감시 대상이 있을 경우 서비스 재시작이 가능하도록 기반을 만든다.

---

## 구현하지 않을 것

* 실제 코레일 접속
* 실제 잔여석 확인
* 자동 로그인
* 자동 예약
* 자동 결제

---

## 완료 조건

실제 Android 기기에서:

* 감시 대상 추가 성공
* 수정 성공
* 삭제 성공
* 저장 성공
* 재실행 후 데이터 유지
* 감시 ON/OFF 성공
* 테스트 알림 성공
* Foreground Service 실행 성공

---

# 9. Phase 2 — 실제 KTX 조회 방식 조사 및 연동 설계

## 목표

실제 KTX 잔여석 확인을 구현하기 전에 가장 안정적이고 유지보수 가능한 접근 방식을 결정한다.

## 중요

Phase 2에서는 **바로 무한 반복 조회부터 구현하지 않는다.**

먼저 실제 서비스 구조와 현재 접근 가능한 방식, Android 환경의 제약을 조사한다.

검토 대상:

* 코레일 공식 Android 앱의 동작
* 공식 웹페이지
* 일반 승차권 조회 흐름
* 로그인 필요 여부
* 비로그인 상태에서 가능한 기능
* 잔여석 정보 표시 방식
* 세션 처리
* 네트워크 응답 구조
* 서버/앱 측 호출 방식

---

## 핵심 판단

다음 중 가장 적절한 방법을 선택한다.

### 방법 A

앱 내부 HTTP 요청

### 방법 B

Android WebView 기반 접근

### 방법 C

공식 앱/웹과 연계하는 사용자 중심 방식

### 방법 D

그 외 합법적이고 안정적인 방법

단순히 "가장 쉽게 스크래핑되는 방법"을 선택하지 않는다.

---

## 안전 원칙

다음은 구현하지 않는다.

* CAPTCHA 우회
* 인증 우회
* anti-bot 우회
* 탐지 회피 로직
* 비정상적인 요청 폭주
* 자동 예약
* 자동 결제

---

## 조회 엔진 인터페이스

실제 구현 전에 인터페이스를 고정한다.

예:

```kotlin
interface MonitoringEngine {
    suspend fun check(target: WatchTarget): CheckResult
}
```

결과:

```text
CheckResult
 ├── Success
 │     ├── trains
 │     └── checkedAt
 │
 ├── NoResult
 └── Error
```

---

## 완료 조건

* 실제 조회 방식 결정
* 장단점 문서화
* API/웹 동작 조사 결과 기록
* MonitoringEngine 인터페이스 확정
* 최소 1회의 실제 조회 검증 성공 또는 기술적으로 불가능한 이유 확인

---

# 10. Phase 3 — 실제 잔여석 판정 엔진

## 목표

실제 조회 결과에서 사용자가 원하는 좌석을 정확히 판정한다.

---

## 조건

최소:

```text
날짜
출발역
도착역
시간 범위
열차 종류
좌석 등급
승객 수
```

을 적용한다.

---

## 예시

사용자 조건:

```text
부산 → 서울
12:00 ~ 18:00
KTX
일반실
1명
```

조회 결과:

```text
11:50 KTX
매진

13:10 KTX
일반실 가능

14:20 KTX
특실 가능
일반실 매진

17:40 KTX-산천
일반실 가능
```

판정 결과:

```text
13:10 → MATCH
14:20 → NO MATCH
17:40 → MATCH
```

---

## 매칭 엔진 분리

다음 형태로 분리한다.

```text
SeatMatchingEngine
```

입력:

```text
WatchTarget
+
TrainResults
```

출력:

```text
MatchedTrain
```

---

## 중복 알림 방지

동일한 열차가 연속 조회에서 계속 발견되어도 무한정 알림을 발생시키지 않는다.

예:

```text
13:10 발견
→ 알림

30초 후
13:10 여전히 존재
→ 중복 알림 억제
```

좌석 상태가 사라졌다가 다시 나타나는 경우에는 다시 알림할 수 있도록 한다.

---

## 완료 조건

* 조건 필터 정상 동작
* 시간 범위 필터 정상
* 열차 종류 필터 정상
* 좌석 등급 필터 정상
* 승객 수 처리 정상
* 중복 알림 억제 정상

---

# 11. Phase 4 — 실시간 감시 + 즉시 알림

## 목표

스마트폰에서 장시간 실제 감시가 가능하도록 만든다.

---

## Foreground Service

실제 MonitoringEngine을 Foreground Service에서 실행한다.

구조:

```text
MonitoringService
    ↓
MonitoringScheduler
    ↓
MonitoringEngine
    ↓
SeatMatchingEngine
    ↓
NotificationManager
```

---

## 감시 주기

사용자가 설정에서 변경 가능하도록 한다.

예:

```text
30초
1분
2분
5분
```

단, 서비스 측 부하와 과도한 반복 요청을 고려한다.

고정된 극단적으로 짧은 요청 주기를 기본값으로 사용하지 않는다.

---

## 네트워크 장애

다음 상태를 구분한다.

```text
정상
조회 실패
인터넷 없음
서버 응답 오류
일시적인 오류
```

일시적 오류에서는 무한 재시도를 하지 않는다.

적절한 backoff를 적용한다.

---

## 알림

좌석 발견 시:

* 높은 중요도
* 소리
* 진동
* 앱 내 이벤트 기록

---

## 완료 조건

* 화면을 꺼도 감시 유지
* 다른 앱을 사용해도 감시 유지
* 오류 발생 후 복구
* 좌석 발견 알림 정상
* 감시 중지 정상
* 앱 강제 종료 및 재실행 시 정상 처리

---

# 12. Phase 5 — 다중 감시 대상 및 고급 조건

## 목표

실사용 편의성을 높인다.

---

## 여러 감시 대상

예:

```text
① 부산 → 서울 9/27 12~18시
② 부산 → 서울 9/27 18~22시
③ 서울 → 부산 9/28 17~22시
```

각각 독립적인 감시 대상으로 관리한다.

---

## 우선순위

감시 대상에 우선순위를 부여할 수 있도록 준비한다.

예:

```text
최우선
높음
보통
```

---

## 추가 조건

필요 시 다음 조건을 추가한다.

* 특정 열차 번호 선호
* 특정 열차 제외
* 출발 시간 우선순위
* 좌석 등급 우선순위
* 여러 열차 중 가장 빠른 것 선택

단, 실제 사용성에 도움이 되는 경우만 추가한다.

---

## 결과 우선순위

예:

```text
1순위
희망 시간에 가까움

2순위
선호 열차

3순위
좌석 등급

4순위
기타
```

정확한 우선순위는 실제 UX를 보고 조정한다.

---

## 완료 조건

* 복수 감시 대상 동시 관리
* 개별 ON/OFF
* 개별 수정/삭제
* 알림이 어느 감시 대상에서 발생했는지 식별 가능
* 우선순위 정상 동작

---

# 13. Phase 6 — 장시간 안정성 / 재부팅 / 배터리 / 네트워크

## 목표

"앱을 켜놓고 잊어버려도 최대한 오래 동작하는 앱"을 만든다.

---

## 재부팅

스마트폰 재부팅 후:

```text
BOOT_COMPLETED
↓
활성 감시 대상 확인
↓
감시 서비스 재시작
```

---

## 배터리 최적화

Android의 배터리 제한으로 인해 서비스가 종료될 가능성을 고려한다.

설정 안내 화면 제공:

```text
배터리 사용 제한 해제 권장
백그라운드 활동 허용 권장
알림 권한 확인
```

기기 제조사별 차이를 고려하되 특정 제조사에 종속적인 해킹 방식은 사용하지 않는다.

---

## 앱 종료

다음을 구분하여 처리한다.

### 사용자가 감시 중지

서비스 종료.

### 앱 UI 종료

감시 유지.

### 시스템에 의한 서비스 종료

가능한 범위에서 복구.

### 스마트폰 재부팅

활성 감시 대상이 있다면 복구.

---

## 네트워크 변화

예:

```text
Wi-Fi → LTE
LTE → Wi-Fi
인터넷 끊김 → 복구
```

시나리오를 테스트한다.

---

## 장시간 테스트

최소:

* 6시간
* 12시간
* 가능하면 24시간

테스트한다.

확인:

* 메모리 사용량 증가 여부
* 배터리 소비
* 서비스 종료 여부
* 네트워크 오류 복구
* 중복 알림
* 데이터 손실

---

# 14. Phase 7 — 실사용 스트레스 테스트

## 목표

실제 명절 취소표 감시에 사용할 수 있는 수준인지 검증한다.

---

## 테스트 시나리오

### Case 1

좌석 없음

```text
매진
→ 계속 감시
```

### Case 2

좌석 발견

```text
매진
→ 좌석 발생
→ 즉시 알림
```

### Case 3

좌석 발생 후 다시 매진

```text
발견
→ 알림
→ 다시 매진
→ 다시 감시
```

### Case 4

좌석 반복 출현

중복 알림이 폭주하지 않아야 한다.

### Case 5

네트워크 끊김

복구 후 감시 재개.

### Case 6

화면 꺼짐

감시 유지.

### Case 7

다른 앱 사용

감시 유지.

### Case 8

스마트폰 재부팅

설정된 활성 감시 복원.

---

## 로그

최종적으로 문제 분석이 가능하도록 최소한의 로그를 남긴다.

예:

```text
10:20 감시 시작
10:21 조회 성공
10:22 조회 성공
10:23 네트워크 오류
10:24 재시도
10:25 좌석 발견
10:25 알림 발송
```

민감한 인증정보나 비밀번호를 로그에 기록하지 않는다.

---

# 15. Phase 8 — 최종 UI/UX 및 배포

## 목표

실사용 가능한 최종 APK를 만든다.

---

## UI 개선

* 메인 화면 단순화
* 감시 상태 명확화
* 좌석 발견 강조
* 최근 이벤트 개선
* 설정 접근성 개선
* 오류 메시지 개선

---

## 상태 표시

최소:

```text
🟢 감시 중

⚪ 중지

🟡 일시적 오류

🔴 감시 불가
```

를 명확히 구분한다.

---

## 좌석 발견 화면

예:

```text
🚨 KTX 좌석 발견

부산 → 서울

9월 27일
14:20
KTX 122

일반실
1석 가능

[예매 화면 열기]
```

---

## 예매 화면 연결

사용자가 버튼을 누르면 적절한 코레일 예매 화면으로 이동하도록 한다.

앱 내부 자동 예매는 하지 않는다.

---

## 배포

최종:

* Debug APK
* Release APK

생성.

버전명과 버전코드를 설정한다.

예:

```text
1.0.0
```

---

# 16. 테스트 원칙

모든 Phase는 다음 순서로 진행한다.

```text
구현
↓
컴파일
↓
자동 테스트 가능 부분 테스트
↓
실제 Android 기기 테스트
↓
문제 수정
↓
완료 보고
```

"코드가 작성되었다"와 "기능이 정상 동작한다"를 동일하게 취급하지 않는다.

---

# 17. 오류 처리 원칙

사용자에게 기술적인 예외를 그대로 보여주지 않는다.

나쁜 예:

```text
java.net.SocketTimeoutException
```

좋은 예:

```text
서버 응답이 늦어 조회에 실패했습니다.
잠시 후 다시 시도합니다.
```

개발 로그에는 상세 오류를 남길 수 있다.

---

# 18. 성능 원칙

모바일 앱이므로 다음을 중요하게 생각한다.

* 불필요한 네트워크 요청 최소화
* 불필요한 UI 재구성 최소화
* 메모리 누수 방지
* 백그라운드 CPU 사용 최소화
* 이미지/리소스 최소화
* 로그 과다 기록 방지

앱이 단순한 감시 앱인데 무겁게 동작하지 않도록 한다.

---

# 19. 보안 원칙

다음 정보는 코드/로그/평문 파일에 저장하지 않는다.

* 코레일 비밀번호
* 세션 정보
* 인증 토큰
* 개인 결제정보

필요한 인증 관련 정보가 있다면 Android의 보안 저장소를 우선 검토한다.

---

# 20. 사용자 경험 원칙

사용자는 기술적인 내부 구현을 볼 필요가 없다.

메인 화면에서 사용자가 즉시 알아야 하는 것은:

```text
지금 감시 중인가?
무엇을 감시하고 있는가?
언제 마지막으로 확인했는가?
문제가 있는가?
좌석이 나왔는가?
```

이다.

---

# 21. 프로젝트에서 하지 않는 것

다음 기능은 명시적으로 프로젝트 범위에서 제외한다.

## 자동 결제

구현하지 않는다.

## 자동 예약 확정

구현하지 않는다.

## CAPTCHA 우회

구현하지 않는다.

## 매크로 탐지 우회

구현하지 않는다.

## 대량 계정 운영

구현하지 않는다.

## 비정상적인 요청 폭주

구현하지 않는다.

## 제3자 유료 서버 의존

특별한 이유가 없는 한 사용하지 않는다.

---

# 22. Phase 완료 보고 형식

각 Phase가 끝나면 반드시 다음 형식으로 보고한다.

```text
# Phase X 완료 보고

## 1. 구현 완료
- ...
- ...
- ...

## 2. 테스트 결과

### Build
성공 / 실패

### Android 설치
성공 / 실패

### 주요 기능
- 기능 A: 성공
- 기능 B: 성공
- 기능 C: 실패

## 3. 발견된 문제
- ...

## 4. 미구현
- ...

## 5. 다음 Phase 준비사항
- ...

## 6. 사용자가 실제로 확인해야 할 사항
- ...
```

---

# 23. 특히 중요한 보고 원칙

다음 표현을 구분한다.

### 구현됨

코드가 존재한다.

### 빌드 성공

컴파일 및 APK 생성이 성공했다.

### 기기 테스트 성공

실제 Android 기기에서 동작 확인했다.

### 장시간 테스트 성공

지정된 시간 동안 안정적으로 동작했다.

이 네 가지를 혼동하지 않는다.

---

# 24. 현재 Phase 관리

프로젝트 루트에 다음 파일을 추가로 둘 수 있다.

```text
CURRENT_PHASE.md
```

예:

```text
현재 Phase: 1
상태: 진행 중
```

Phase 완료 후:

```text
현재 Phase: 1
상태: 완료
```

사용자가 다음 Phase를 지시하면:

```text
현재 Phase: 2
상태: 진행 중
```

으로 변경한다.

---

# 25. Antigravity에 대한 최종 실행 규칙

이 문서를 프로젝트의 장기 개발 계획으로 사용한다.

**사용자가 특정 Phase를 지시하지 않았다면 임의로 다음 Phase로 넘어가지 않는다.**

현재 작업의 범위를 판단할 때:

1. PROJECT_PLAN.md 확인
2. 사용자가 요청한 Phase 확인
3. 해당 Phase 목표 확인
4. 해당 Phase의 완료 조건 확인
5. 현재 코드 상태 분석
6. 필요한 최소 변경 적용
7. 빌드
8. 테스트
9. 완료 보고

순으로 진행한다.

---

# 26. 예시 명령

사용자는 다음과 같이 짧게 지시할 수 있다.

```text
Phase 0 진행해.
```

또는:

```text
PROJECT_PLAN.md 기준으로 Phase 1만 진행해.
```

또는:

```text
Phase 1에서 발견된 오류만 수정해.
다음 Phase는 시작하지 마.
```

또는:

```text
Phase 2 진행.
PROJECT_PLAN.md의 범위 안에서만 구현해.
```

---

# 27. 최종 프로젝트 성공 조건

프로젝트 최종 성공은 다음 상태를 의미한다.

```text
Android 스마트폰
      ↓
앱 실행
      ↓
감시 대상 설정
      ↓
감시 시작
      ↓
화면을 꺼도 감시
      ↓
다른 앱을 사용해도 감시
      ↓
네트워크 일시 장애 복구
      ↓
스마트폰 재부팅 후 감시 복원
      ↓
원하는 KTX 좌석 발견
      ↓
즉시 소리/진동/알림
      ↓
예매 화면 열기
      ↓
사용자가 최종 예약
```

이 전체 흐름이 실제 기기에서 안정적으로 동작하면 프로젝트 완료로 판단한다.

---

# 28. 현재 시작 지점

이 문서를 처음 프로젝트에 적용할 경우:

**Phase 0부터 시작한다.**

Phase 0 완료 이후 사용자의 명시적 지시가 있을 때만 Phase 1을 시작한다.

전체 계획을 미리 읽고 있어도 현재 Phase 외의 기능을 선행 구현하지 않는다.
